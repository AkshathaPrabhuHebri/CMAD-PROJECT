version: "3.7"

volumes:
  db-data:

services:
  server:
    image: myselfakshatha/syslog-server
    ports:
      - 5000:8080
    depends_on:
      -

#  redis:
#    image: redis:alpine

#  worker:
#    image: schoolofdevops/vote-worker
#    depends_on:
#      - redis

  db:
    image: postgres:9.4
    volumes:
      - db-data:/var/lib/postgresql/data

#  result:
#    image:   myselfakshatha/vote
#    ports:
#      - 5001:80

